# CHANGELOG

История выполненного. PRD.md и AGENTS.md описывают только актуальное состояние и то, что ещё не сделано.

## Фаза 6 — Категории

### 2026-09-22
- ✅ Таблица `categories` (базовые + пользовательские) и поле `category_id` в `transactions`.
- ✅ Кнопки категорий в `/minus` и `/plus`: базовая + `[➕ Своя]` + `[❌ Без категории]`; категорию можно указать в команде (`/minus 5000 Продукты`).
- ✅ Команда `/categories`: список по типам, добавление (расходы) и удаление только своих категорий.
- ✅ Блок «Топ категорий за месяц» в `/stats` (топ-3 трат, календарный месяц, скрыт при отсутствии).

## Фаза 5.1 — Фиксы

### 2026-09-22
- ✅ Пункт 1: Название карты при онбординге + команда /accounts. Шаг названия карты в онбординге (пусто → «Карта»), `/accounts` показывает счета (в семье — обоих) и переименовывает карту, копилка не переименовывается.
- ✅ Пункт 3: Разделение статистики по участникам. В `/stats` для семьи кнопки `[📊 Общая]` `[👤 Имя]`, переключение видов через `edit_message_text` (не новое сообщение). «Общая» — счета обоих, свободно по участникам и итого, общие цели и операции. «Моя» — только счета/платежи/цели/операции участника. У одиночки кнопок нет.
- ✅ Вердикт «хватает / не хватает» в `/stats`: блок «Платежи до конца месяца» с вердиктом по каждому платежу (только по балансу карты владельца, доход не учитывается; накопление внутри дня, сброс между днями). В «Общей» — с именем владельца, в «Моей» и у одиночки — без.
- ✅ Фаза 5.1 завершена: все фиксы Фазы 5 закрыты.

### 2026-09-21
- ✅ Пункт 1: Заголовки в /stats (Markdown *Заголовок*).
- ✅ Пункт 2: Код семьи отдельным сообщением + кнопка [🔑 Показать код], приглашение через диплинк.
- ✅ Пункт 6: Кнопки в /goals allocate (реализовано в goals.py).
- ✅ Исправлен баг: при вступлении в семью по диплинку сумма из онбординга терялась (счета предсоздавались с нулём). Теперь счета нового участника создаёт онбординг — сразу с балансом карты и `family_id`.

## Фаза 5 — Семейный бюджет
- ✅ Таблицы families, family_members.
- ✅ /family create, /family join, /family.
- ✅ Общая прозрачность счетов.
- ✅ Общие цели.

## Фаза 4 — Копилка, Цели, Прогноз
- ✅ Таблица accounts (card / savings).
- ✅ /savings, /savings add.
- ✅ /goals, /goals add, /goals allocate, /goals edit, /goals del.
- ✅ /forecast.
- ✅ Редактирование долгов.

## Фаза 3 — Долги
- ✅ Таблица debts.
- ✅ /debts, /debts add, /debts del.
- ✅ Ближайшие платежи в /stats.

## Фаза 2 — Операции
- ✅ /minus, /plus, /correct.
- ✅ /stats.
- ✅ Menu Button.

## Фаза 1 — Онбординг
- ✅ Каркас проекта, SQLite через SQLAlchemy async.
- ✅ Онбординг: деньги, формат дохода, даты и суммы.
- ✅ /stats.
