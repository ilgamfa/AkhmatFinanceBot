# PRD: Карманный финсоветник (Telegram-бот)

**Версия:** 2.0
**Дата:** 2026-09-21
**Автор:** Владелец продукта
**Статус:** ready-for-agent

---

## 1. Проблема

Я веду бюджет в Excel, но это неудобно:
- Нет ответа на вопрос «могу ли я купить X прямо сейчас?».
- Кредиты, ипотека, рассрочки и кредитка разбросаны и не складываются в единую картину.
- Нет прогноза на будущее — я вижу только прошлые траты.
- Нет ощущения, что я делаю всё правильно.

Существующие приложения (Дзен-мани, CoinKeeper) показывают **прошлое**, а мне нужно **будущее**: что мне предстоит, сколько свободно и что я могу себе позволить.

---

## 2. Цель продукта

Telegram-бот «Карманный финсоветник», который за 5 секунд даёт ответ:
> «Сколько у меня свободных денег, что скоро платить, и могу ли я купить телевизор?»

Бот смотрит **вперёд**, а не назад. Он не требует разносить каждую трату по 20 категориям. Он даёт мягкие советы и предупреждает, если покупка ломает план.

---

## 3. Целевой пользователь

**Персона:** Я сам и моя жена (семейный бюджет).
- Веду бюджет в Excel, но хочу автоматизации.
- Есть кредит (46 000 ₽/мес), ипотека (61 000 ₽/мес), кредитка, рассрочки.
- Есть цели: машина, подушка безопасности, отпуск.
- Хочу простой интерфейс: зашёл в бота — увидел сводку.
- Не хочу автоподгрузку из банков — вношу данные сам.
- Готов платить за хостинг (~500–1000 ₽/мес).

---

## 4. User Stories

1. **Как пользователь**, я хочу зайти в бота и сразу увидеть, сколько у меня свободных денег.
2. **Как пользователь**, я хочу видеть 3–5 ближайших платежей.
3. **Как пользователь**, я хочу видеть прогресс по целям.
4. **Как пользователь**, я хочу добавить трату одной командой, не выбирая категорию.
5. **Как пользователь**, я хочу спросить «могу ли купить телевизор за 40 000?» и получить ответ.
6. **Как пользователь**, я хочу получать вечернее напоминание внести траты.
7. **Как пользователь**, я хочу получать мягкие советы.
8. **Как пользователь**, я хочу видеть контроль по кредитке.
9. **Как пользователь**, я хочу вести семейный бюджет с женой: видеть её счета и траты, общие цели.

---

## 5. Функциональные требования

### 5.1. Онбординг

При `/start`:
1. Приветствие: «Привет! Я Карманный финсоветник.»
2. Сколько денег сейчас (карты, наличные).
3. **Название карты** (обязательно). Например: «Т-Банк», «Сбер».
4. Формат дохода: фиксированный / нерегулярный.
   - Фиксированный: 1 или 2 раза в месяц, даты и суммы.
   - Нерегулярный: средняя сумма в месяц.
5. Создаются два счёта: `card` (с названием) и `savings` (название по умолчанию «Копилка»).

Семья создаётся отдельно через `/family create` или `/family join <код>`.

### 5.2. Команды

| Команда | Действие |
|---|---|
| `/start` | Онбординг |
| `/stats` | Главный экран: сводка и статистика |
| `/minus [сумма]` | Добавить трату |
| `/plus [сумма]` | Добавить доход |
| `/correct [сумма]` | Скорректировать баланс карты |
| `/debts` | Мои долги |
| `/savings` | Моя копилка |
| `/goals` | Мои цели |
| `/forecast` | Прогноз |
| `/family` | Семья |
| `/refresh` | Сбросить и начать заново |

Без аргумента — ForceReply и приём следующего сообщения как суммы.

### 5.3. Главный экран (`/stats`)

Формат:

*Свободно:* 200 800 ₽

*Счета:*
Т-Банк (моя): 50 000 ₽
Сбер (жены): 30 000 ₽

*Ближайшие платежи:*
25.09 — Кредит: 46 000 ₽
30.09 — Ипотека: 61 000 ₽

*Цели:*
🏖 Отпуск: 400 000 / 500 000 ₽ (80%)

*Последние операции:*
−3000 ₽ (жена, сегодня)
+50 000 ₽ (я, вчера)

*За месяц:* +47 000 ₽

### 5.4. Долги (Фаза 3)

- Универсальный паттерн: название, тип, сумма, день месяца.
- Типы: `loan`, `mortgage`, `credit_card`, `installment`.
- Без расчёта грейс-периода. Только дата и сумма платежа.
- Без остатка долга.

### 5.5. Копилка и Цели (Фаза 4)

- Одна копилка (`savings`) на пользователя.
- Цели привязываются к `family_id` (если в семье) или `telegram_id`.
- Прогресс цели = сумма `allocations` с обоих `savings`.
- `/savings add N` — переводит N с карты в копилку.
- `/savings correct N` — выравнивает баланс копилки.
- `/goals allocate` — закрепить за целью.

### 5.6. Семейный бюджет (Фаза 5)

- Семья объединяет двух пользователей.
- Приглашение через код.
- У каждого свои счета (`card` + `savings`), но оба видят счета друг друга.
- Цели общие для семьи.
- Полная прозрачность.

### 5.7. Советы (Фаза 7)

- Стиль: мягкий. Не запрещает, а предупреждает.
- Пример: «Если купишь телевизор сейчас, подушка сдвинется на 2 месяца.»

### 5.8. Напоминания (Фаза 7)

- Вечернее сообщение: «Как прошёл день? Не забудь записать траты».

### 5.9. Модель данных

| Таблица | Назначение |
|---|---|
| `users` | Пользователь: `advice_style`, `onboarding_completed`, `income_type`, `income_dates`, `income` |
| `transactions` | Операции: `type` (`expense`/`income`/`correction`/`savings_add`), `account_id` |
| `debts` | Долги: кредит, ипотека, кредитка, рассрочка |
| `accounts` | Счета: `type` (`card`/`savings`), `name`, `balance`, `family_id` |
| `goals` | Цели: `name`, `target`, `deadline`, `priority`, `family_id` |
| `allocations` | Связи: `goal_id`, `telegram_id`, `amount` |
| `families` | Семьи: `name`, `invite_code` |
| `family_members` | Участники: `family_id`, `telegram_id`, `role`, `first_name` |

UNIQUE-ограничение на `accounts (telegram_id, type)`.
Суммы — целые рубли.

---

## 6. Нефункциональные требования

| Требование | Значение |
|---|---|
| **Платформа** | Telegram |
| **Хранение данных** | Только на серверах в РФ (152-ФЗ) |
| **База данных** | SQLite (MVP), PostgreSQL (позже) |
| **Язык** | Python 3.12+ |
| **Библиотека** | aiogram 3.x |
| **ORM** | SQLAlchemy 2.0 async + aiosqlite |
| **Parse mode** | legacy Markdown |
| **Хостинг** | VPS в РФ |
| **Автоподгрузка банков** | Нет |
| **Скорость ответа** | ≤ 2 секунды на `/stats` |

---

## 7. Границы MVP

- ❌ Автоподгрузка из банков.
- ❌ Собственная ML-модель.
- ❌ Инвестиционный учёт и налоги.
- ❌ WhatsApp и другие мессенджеры.
- ❌ Веб-интерфейс.
- ❌ Несколько карт у одного пользователя.
- ❌ Скрытые счета.

---

## 8. Метрики успеха

| Метрика | Цель |
|---|---|
| Время ответа на `/stats` | ≤ 2 сек |
| Я пользуюсь ботом | Каждый день |
| Точность прогноза | Ошибка ≤ 5% |
| Пропущенных платежей | 0 |

---

## 9. Риски

| Риск | Митигация |
|---|---|
| Нарушение 152-ФЗ | Уведомление РКН, хранение в РФ |
| Агент ломает логику | TDD: тесты до кода |
| Потеря контекста агентом | `AGENTS.md` + `PRD.md` в `instructions` |
| Спонтанные покупки | Мягкие советы + прогноз |

---

## 10. Тестирование (Seams)

**Самый высокий шов:** обработчики команд.
- `/minus 5000` → баланс `card` уменьшился.
- `/stats` → сводка содержит свободные деньги, платежи, цели.
- `/family create` → семья создана, код отдельным сообщением.

Тесты пишутся **до** реализации (TDD).

---

## 11. Технический стек

| Компонент | Технология |
|---|---|
| Язык | Python 3.12+ |
| Библиотека | aiogram 3.x |
| Конфиг | python-dotenv (`.env`) |
| ORM | SQLAlchemy 2.0 async + aiosqlite |
| БД | SQLite |
| Parse mode | legacy Markdown |
| Хостинг | VPS в РФ |
| LLM | API (Фаза 8) |
| Тесты / линтер | pytest / ruff |
| IDE | VS Code |
| Агент | OpenCode + DeepSeek Flash |

---

## 12. План разработки (фазы)

| Фаза | Название | Статус |
|---|---|---|
| 1 | Онбординг + `/stats` + SQLite | ✅ |
| 2 | `/minus`, `/plus`, `/correct`, `/stats` | ✅ |
| 3 | Долги (`/debts`) | ✅ |
| 4 | Копилка + Цели + Прогноз | ✅ |
| 5 | Семейный бюджет (`/family`) | ✅ |
| 5.1 | Фиксы Фазы 5 | 🔄 |
| 6 | Категории | ⏳ |
| 7 | `/can`, советы, напоминания (`/advice`) | ⏳ |
| 8 | LLM-советник, экспорт (`/export`) | ⏳ |

### Фаза 5.1 — Фиксы (в работе)

1. Заголовки в `/stats` (Markdown: `*Заголовок*`). *(в работе)*
2. Код семьи отдельным сообщением + кнопка `[🔑 Показать код]`.
3. Название карты при онбординге (обязательно).
4. Улучшение создания семьи (кнопки при `/start`).
5. «Хватает / не хватает» в `/stats`.
6. Кнопки в `/goals allocate`.

### Фаза 6 — Категории

- Таблица `categories`.
- Кнопки категорий в `/minus` и `/plus`.
- Топ категорий в `/stats`.

### Фаза 7 — Советы

- `/can`, `/advice`, напоминания.

### Фаза 8 — LLM

- Нейросеть для советов, `/export`.

---

**Конец документа.**