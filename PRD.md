# PRD: Карманный финсоветник (Telegram-бот)

**Версия:** 1.0 (MVP)
**Дата:** 2026-09-19
**Автор:** Владелец продукта
**Статус:** ready-for-agent

---

## 1. Проблема

Я веду бюджет в Excel, но это неудобно:
- Нет ответа на вопрос «могу ли я купить X прямо сейчас?».
- Кредиты, ипотека, рассрочки и кредитка разбросаны и не складываются в единую картину.
- Нет прогноза на будущее — я вижу только прошлые траты.
- Нет ощущения, что я делаю всё правильно.

Существующие приложения (Дзен-мани, CoinKeeper) показывают **прошлое**, а мне нужно **будущее**: что мне предстоит, сколько свободно и что я могу себе позволить.

---

## 2. Цель продукта

Telegram-бот, который за 5 секунд даёт ответ:
> «Сколько у меня свободных денег, что скоро платить, и могу ли я купить телевизор?»

Бот смотрит **вперёд**, а не назад. Он не требует разносить каждую трату по 20 категориям. Он даёт мягкие советы и предупреждает, если покупка ломает план.

---

## 3. Целевой пользователь

**Персона:** Я сам.
- Веду бюджет в Excel, но хочу автоматизации.
- Есть кредит (46 000 ₽/мес, 51 мес), ипотека (61 000 ₽/мес), кредитка с разными платежами, рассрочки на маркетплейсах.
- Есть цели: машина, подушка безопасности.
- Хочу простой интерфейс: зашёл в бота — увидел сводку.
- Не хочу автоподгрузку из банков — вношу данные сам.
- Готов платить за хостинг (~500–1000 ₽/мес).

---

## 4. User Stories

1. **Как пользователь**, я хочу зайти в бота и сразу увидеть, сколько у меня свободных денег, чтобы понять, могу ли я что-то купить.
2. **Как пользователь**, я хочу видеть 3–5 ближайших платежей (кредит, ипотека, кредитка), чтобы не пропустить дату.
3. **Как пользователь**, я хочу видеть прогресс по целям (машина, подушка), чтобы понимать, сколько осталось.
4. **Как пользователь**, я хочу добавить трату одной командой (`/add 5000`), не выбирая категорию.
5. **Как пользователь**, я хочу спросить «могу ли купить телевизор за 40 000?», и получить ответ с учётом моего плана.
6. **Как пользователь**, я хочу получать вечернее напоминание внести траты за день.
7. **Как пользователь**, я хочу получать мягкие советы: «Если купишь сейчас — подушка сдвинется на 2 месяца».
8. **Как пользователь**, я хочу видеть особый контроль по кредитке: сколько потратил, сколько нужно вернуть до конца грейс-периода.

---

## 5. Функциональные требования (MVP)

### 5.1. Онбординг
При первом `/start` бот показывает вводный экран: объясняет, зачем нужны данные (3 вопроса) и предлагает кнопки **[Начать]** / **[Позже]**.

Флоу Фазы 1 (выбор — только кнопками, свободный текст — только числа):
1. **Деньги сейчас** — «Сколько у тебя сейчас на картах и наличных?» → `users.free_money`.
2. **Формат дохода** — кнопки **[Фиксированная]** / **[Нерегулярная]** → `users.income_type`.
3. **Сумма и даты**:
   - «Фиксированная» → **[1 раз]** / **[2 раза]** → ввод «дата, сумма» (например «10, 50000») → `users.income_dates` (JSON).
   - «Нерегулярная» → средняя сумма в месяц → `users.income`.
4. Финал: «Готово! Теперь отправь /stats, чтобы увидеть сводку.»

Кнопка **[Позже]** пропускает онбординг: профиль остаётся пустым, но `onboarding_completed = true`.
Формулировки про аванс больше не используются.

Данные будущих фаз собираются позже: обязательства — Фаза 3, цели — Фаза 4, стиль советов — Фаза 5.

### 5.2. Сводка (`/stats`)
Главная команда вывода. Формат (Фаза 3):

```
Свободно: 200 800 ₽

Доход:
5 числа — 175 000 ₽
20 числа — 175 000 ₽

Ближайшие платежи:
25.09 — Кредит: 46 000 ₽
30.09 — Ипотека: 61 000 ₽

Свободно до ЗП: 93 800 ₽
Свободно до конца месяца: −106 200 ₽

Последние операции:
+25 900 ₽ (сегодня)
−100 ₽ (сегодня)

За сегодня: +25 800 ₽
За неделю: +25 800 ₽
За месяц: +25 800 ₽
```

- **Свободно** — текущий баланс (`users.free_money`).
- **Доход** — по строкам, без скобок и запятых (чтобы Telegram не считал ссылкой). Фиксированный: «N числа — сумма» по каждой дате; нерегулярный: «Доход: нерегулярный» + «Среднее в месяц: …». Если дохода нет — блок скрывается.
- **Ближайшие платежи** — обязательные платежи на ближайшие 30 дней; блок скрывается, если долгов нет.
- **Свободно до ЗП** — баланс минус платежи до следующей зарплаты. Показывается только при наличии долгов и для фиксированного дохода.
- **Свободно до конца месяца** — баланс минус все платежи до конца текущего календарного месяца. Показывается только при наличии долгов.
- Отрицательные суммы показываются как есть.
- Показываются 3–5 последних операций; если операций нет — блок «Последние операции» скрывается.
- Блок «За период» — сальдо (доход + корректировки − траты) за день/неделю/месяц; показывается, если есть хотя бы одна операция.


### 5.3. Команды
| Команда | Действие | Фаза |
|---|---|---|
| `/start` | Онбординг | 1 |
| `/cancel` | Прервать текущий диалог | 1 |
| `/minus 5000` | Трата | 2 |
| `/plus 100000` | Доход | 2 |
| `/correct 15000` | Выравнивание баланса | 2 |
| `/stats` | Сводка: баланс, доход, операции, суммы за период | 2 |
| `/debts` | Список долгов | 3 |
| `/debts add` | Добавить долг пошагово | 3 |
| `/debts del <id>` | Удалить долг | 3 |
| `/refresh` | Сбросить данные и начать заново | 3 |
| `/goals` | Список целей | 4 |
| `/forecast` | Прогноз на месяц/год | 4 |
| `/can 40000` | «Могу ли купить?» | 5 |
| `/advice` | Совет | 5 |
| `/export` | Экспорт данных | 6 |

Отдельной команды `/income` нет — доход вносится через `/plus`. Отдельного `/main` нет — его роль выполняет `/stats`.
Команды `/minus`, `/plus` и `/correct` работают и без аргумента: бот отвечает подсказкой с ForceReply и принимает следующее сообщение как сумму.

### 5.3.1. Операции Фазы 2

**`/minus`** — трата:
- `/minus 5000` → списывает сумму из `users.free_money`, пишет операцию `expense`.
- `/minus` без аргумента → «Напиши сумму траты. Например: 5000» с ForceReply; следующее сообщение принимается как сумма.
- Если сумма не распознана → «Не понял сумму. Напиши число, например: 5000».
- Если баланс ушёл в минус — предупреждение с советом использовать `/correct`.

**`/plus`** — доход:
- `/plus 100000` → прибавляет сумму к `users.free_money`, пишет операцию `income`.
- `/plus` без аргумента → «Напиши сумму дохода. Например: 100000» с ForceReply.

**`/correct`** — выравнивание баланса: пользователь пишет новую сумму свободных денег; бот обновляет `free_money` и пишет операцию `correction` с разницей. Без аргумента — ForceReply. Категории не спрашиваются.

Знак зашит в команду, поэтому вручную его вводить не нужно.

### 5.4. Долги (Фаза 3)
Долг — это ежемесячный обязательный платёж: название, тип, сумма платежа, день месяца.
Типы: `loan` (кредит), `mortgage` (ипотека), `credit_card` (кредитка), `installment` (рассрочка).
Кредитка — такой же ежемесячный платёж; грейс-период и остаток долга не считаются (только дата и сумма ближайшего платежа).

**`/debts`** — список долгов с номерами и итогом в месяц:
```
Твои долги:

1. Кредит — 46000 ₽, 25 числа
2. Ипотека — 61000 ₽, 30 числа

Итого в месяц: 107000 ₽
```
Если долгов нет — «У тебя пока нет долгов. Добавь через /debts add».

**`/debts add`** — пошаговое добавление через ForceReply:
1. «Название долга? Например: Кредит»
2. «Тип долга?» — кнопки [Кредит] [Ипотека] [Кредитка] [Рассрочка]
3. «Сумма платежа? Например: 46000»
4. «День месяца? Например: 25»

После добавления: «Долг добавлен: Кредит — 46000 ₽, 25 числа».

**`/debts del <id>`** — удаляет долг по номеру: «Долг удалён». Если id не найден — «Долг с таким номером не найден».

### 5.5. Цели (Фаза 4)
- Паттерн: название, сумма, срок.
- Бот считает, сколько нужно откладывать в месяц.
- Прогресс показывается на `/stats` и `/goals`.

### 5.6. Советы (Фаза 5)
- **Стиль:** мягкий. Не запрещает, а предупреждает и предлагает альтернативы.
- Пример: «Если купишь телевизор сейчас, подушка сдвинется на 2 месяца. Может, подождёшь до следующего месяца?»

### 5.7. Напоминания (Фаза 5)
- Вечернее сообщение: «Как прошёл день? Не забудь записать траты».
- Ежедневная/еженедельная выгрузка (по запросу).

### 5.8. Модель данных (MVP)

| Таблица | Назначение |
|---|---|
| `users` | Пользователь и его профиль: `free_money`, `income_type` (`fixed`/`irregular`), `income_dates` (JSON), `income` (средний доход) |
| `transactions` | Операции Фазы 2: `telegram_id`, `type = expense / income / correction`, `amount` (целые рубли), `created_at` (ISO). Без категорий |
| `debts` | Обязательные платежи Фазы 3: `telegram_id`, `name`, `type` (`loan`/`mortgage`/`credit_card`/`installment`), `amount` (целые рубли), `payment_day` (1–31), `created_at` (ISO) |
| `goals` | Цели (Фаза 4) |

---

## 6. Нефункциональные требования

| Требование | Значение |
|---|---|
| **Платформа** | Telegram (единственный мессенджер) |
| **Хранение данных** | Только на серверах в РФ (152-ФЗ) |
| **База данных** | SQLite (MVP), PostgreSQL (позже) |
| **Язык** | Python (aiogram 3.x) |
| **Хостинг** | VPS в РФ (~500–1000 ₽/мес) |
| **Автоподгрузка банков** | Нет (ввод вручную) |
| **Скорость ответа** | ≤ 2 секунды на `/stats` |

---

## 7. Границы MVP (что НЕ делаем)

- ❌ Автоподгрузка из банков.
- ❌ Собственная ML-модель (используем LLM через API).
- ❌ Инвестиционный учёт и налоги.
- ❌ Сложные сценарии «что если».
- ❌ WhatsApp и другие мессенджеры.
- ❌ Веб-интерфейс.

---

## 8. Метрики успеха

| Метрика | Цель |
|---|---|
| Время ответа на `/stats` | ≤ 2 сек |
| Я пользуюсь ботом | Каждый день |
| Точность прогноза свободных денег | Ошибка ≤ 5% |
| Количество пропущенных платежей | 0 |
| Субъективное ощущение контроля | «Я знаю, что делаю» |

---

## 9. Риски и как их закрываем

| Риск | Митигация |
|---|---|
| Нарушение 152-ФЗ | Уведомление РКН, хранение в РФ, согласие в боте |
| Агент ломает логику расчётов | TDD: тесты до кода |
| Потеря контекста агентом | `AGENTS.md` + `PRD.md` в `instructions` |
| Страховка/кредитка съедают бюджет | Отдельный контроль грейс-периода |
| Спонтанные покупки | Мягкие советы + прогноз |

---

## 10. Тестирование (Seams)

**Самый высокий шов:** обработчики команд.
- Подаём `/minus 5000` → проверяем, что баланс изменился и операция записана.
- Подаём `/stats` → проверяем, что сводка содержит баланс, платежи и суммы за период.
- Подаём `/debts add` → проверяем, что долг записан и виден в `/debts`.
- Подаём `/correct 12000` → проверяем, что баланс обновлён.
- Подаём `/can 40000` → проверяем, что ответ учитывает цели и обязательства (Фаза 5).

Тесты пишутся **до** реализации (TDD).

---

## 11. Технический стек

| Компонент | Технология |
|---|---|
| Язык | Python 3.12+ |
| Библиотека | aiogram 3.x |
| Конфиг | python-dotenv (`.env`) |
| БД | SQLite (MVP), PostgreSQL (позже) |
| Хостинг | VPS в РФ |
| LLM для советов | API (YandexGPT / ProxyAPI) |
| Тесты / линтер | pytest / ruff |
| IDE | VS Code |
| Агент | OpenCode + DeepSeek Flash |

---

## 12. План разработки (фазы)

1. **Фаза 1:** Онбординг + SQLite. *(завершена)*
2. **Фаза 2:** `/minus`, `/plus`, `/correct`, `/stats`. *(текущая)*
3. **Фаза 3:** Долги, кредитка, грейс-период (`/debts`).
4. **Фаза 4:** Цели и прогноз (`/goals`, `/forecast`).
5. **Фаза 5:** `/can`, советы, напоминания (`/advice`).
6. **Фаза 6:** LLM-советник, экспорт (`/export`).

---

**Конец документа.**